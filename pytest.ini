[pytest]
# Blender も Unity も要らない純 Python のテスト (#68)。python -m pytest で数秒。
# テストのファイル名は blender/tests と tools/tests で重ならないようにする。
testpaths = blender/tests tools/tests
addopts = --import-mode=importlib
